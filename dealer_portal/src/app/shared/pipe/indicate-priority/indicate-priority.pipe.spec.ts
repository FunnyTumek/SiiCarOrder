import { IndicatePriorityPipe } from './indicate-priority.pipe';

describe('IndicatePriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new IndicatePriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
